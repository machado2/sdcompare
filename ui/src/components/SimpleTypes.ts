export interface Style {
    id: number;
    name: string;
}

export interface Category {
    id: number;
    name: string;
}

export interface Prompt {
    id: number;
    text: string;
}

export interface Option {
    value: string;
    label: string;
}